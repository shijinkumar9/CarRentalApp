apiVersion: v1
kind: Service
metadata:
  name: backend
spec:
  selector:
    app: carrental-backend
  ports:
    - protocol: TCP
      port: 4000
      targetPort: 4000
  type: ClusterIP   # internal service for frontend to talk to
